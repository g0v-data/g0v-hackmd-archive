5/6上課筆記

重構三口訣
在同一個型別重複的時候，會擷取成方法
在跨型別重複時，會擷取成一個型別
在跨組件重複時，會擷取成另一個組件

解決三種"分支"運算的方法
1、搜尋法取代分支
2、多行取代分支
3、演算法取代分支

宣告在在類別內稱為欄位(Field)
宣告在在方法上稱為參數
宣告在方法內稱為區域變數

個體差異化-執行個體(instance)設定
動態:數值隨每個不同的個體會不同
(static)靜態:每個個體都擁有共同的數值


