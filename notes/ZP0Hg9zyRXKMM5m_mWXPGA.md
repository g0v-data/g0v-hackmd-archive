# 2/13

# 終端機
當機q
control w 刪一段文字
control u 刪全部
contral r 搜尋
control l 清畫面
clear 清畫面
＾上個指令

# git
git config --list 初始化
git config --global.user.name 設定名字
git config --global.user.email 設定mail

git status 看狀態
git add 加檔
git commit m-"檔案名"
git restore
git log 看紀錄
git log --oneline 簡化紀錄
git checkout id 跳到各個版本
vscode git graph 直接看歷史紀錄 更動